import { FC } from "react";

import Container from "components/Container";

const Loader: FC = () => {
  return (
    <Container>
      <div>Carregando...</div>
    </Container>
  );
};

export default Loader;
